#include <stdio.h>

/**
 * main - Entry point
 *
 * description: print the string s without using printf
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char *s = "\"Programming is like building a multilingual puzzle";

	puts(s);
	return (0);
}
