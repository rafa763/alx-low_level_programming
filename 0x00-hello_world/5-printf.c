#include <stdio.h>

/**
 * main - Entry point
 *
 * description: print the string s using printf
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char *s = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", s);
	return (0);
}
